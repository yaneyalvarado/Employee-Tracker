const inquirer = require('inquirer');
const db = require("./sql");
require("console.table");

const promptUser = () => {
    inquirer.prompt([
        {
            name:
            message:
            list:
            choices:
        }
    ])
}